{
  "name": "module-tournament-tree",
  "version": "1.1.3",
  "description": "Module that provides GFX elements for a tournament tree",
  "author": "RCVolus",
  "license": "MIT",
  "toolkit": {
    "modes": [
      "PLUGIN"
    ],
    "plugin": {
      "main": "dist/plugin.js"
    },
    "needsBuild": true
  },
  "devDependencies": {
    "@rcv-prod-toolkit/types": "github:rcv-prod-toolkit/types",
    "typescript": "^4.3.2",
    "sass": "^1.34.1"
  },
  "scripts": {
    "compile-sass": "sass frontend/style/tournament_tree.scss frontend/style/tournament_tree.css",
    "watch-sass": "sass --watch frontend/style/:frontend/style/",
    "build": "npm run compile-sass && tsc"
  }
}
